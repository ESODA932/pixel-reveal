/**
 * Copy this file to config.js and fill in your Supabase project URL and anon key.
 * Create a table `leaderboard` (see README) with Row Level Security for insert/select.
 */
export const SUPABASE_URL = "https://YOUR_PROJECT.supabase.co";
/** Use the anon (legacy JWT) or publishable key from Supabase → Settings → API */
export const SUPABASE_ANON_KEY = "";
